# macOS
.DS_Store

# Confidential case files — privileged work product
Cases/
benchmarks/

# Environment — never commit
.env
.env.save
.env.save.*
.envy
.env.local
.env.*.local
.env.backup
.env.bak

# Node
node_modules/
dist/
webapp/uploads/

# Editor
*.swp
*.swo
*~
.case

# Privileged case material — never commit
Cases/
benchmarks/
Internal-IP/
*.docx
image*.jpg
.legion/

# Claude Code — local-only settings (may contain tokens)
.claude/settings.local.json
webapp/.claude/settings.local.json

# Meeting transcripts and summaries — touch case names + privileged commentary
meetings/
